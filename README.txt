To build it you need to have npm installed, so you can run a command:

npm install

To build the project you need to run:

npm run build

To run the project locally:

npm run start

and open localhost defined in the command window. Usually 8080 port.